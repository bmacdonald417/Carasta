name: carmunity_app
description: Carmunity by Carasta — companion client for iOS and Windows; same social identity and APIs as carasta.com.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  dio: ^5.7.0
  http_parser: ^4.1.2
  flutter_riverpod: ^2.6.1
  intl: ^0.19.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
